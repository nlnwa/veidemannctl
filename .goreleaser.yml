builds:
  - goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
    hooks:
      pre: go generate

archives:
  - id: binary
    format: binary

release:
  draft: true
  prerelease: true
